# Print N to 1 Without Loop

> **Difficulty**: easy  
> **Source**: GeeksforGeeks  
> **Tags**: recursion, algorithms

---

## 📝 Problem Statement

Given an integer `N`, print all natural numbers from `N` down to `1` using recursion — without using any loops. That is, output should start with `N` and decrement down to `1`. ([GfG – Print n to 1 without loop](https://www.geeksforgeeks.org/dsa/print-n-to-1-without-loop/))

---

## 📥 Input

- `N`: a positive integer (1 ≤ N ≤ reasonable recursion‑depth limit)

---

## 📤 Output

- Print the numbers from `N` to `1`, each separated by a space (or newline), in descending order.

---

## 🧪 Example

**Input:**
N = 5
**Output:**  
5 4 3 2 1

---

## 🛠️ Approach

Use recursion with a base case and decrementing argument
